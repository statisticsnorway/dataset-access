webserver:
  port: -1
  host: "0.0.0.0"

grpcserver:
  port: -1

pgpool:
  connect-options:
    port: 10102
    host: localhost
    database: rdc
    user: rdc
    password: rdc
  pool-options:
    max-size: 5

flyway:
  url: jdbc:postgresql://localhost:10102/rdc
  user: rdc
  password: rdc

init:
  db-connectivity-attempts: 30
